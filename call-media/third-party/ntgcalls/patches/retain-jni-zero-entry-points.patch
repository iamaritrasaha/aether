--- a/cmake/FindWebRTC.cmake
+++ b/cmake/FindWebRTC.cmake
@@ -103,10 +103,16 @@ if(NOT TARGET WebRTC::webrtc)
             string(REGEX REPLACE "\n" ";" ELF_DUMP "${ELF_DUMP}")
             set(LD_FLAGS)
             foreach(line ${ELF_DUMP})
-                if (line MATCHES "Java_org_webrtc_")
-                    string(REGEX REPLACE " +" ";" line "${line}")
-                    list(GET line 8 func)
-                    list(APPEND LD_FLAGS "-Wl,--undefined=${func}")
+                if (line MATCHES "Java_")
+                    string(REGEX REPLACE " +" ";" fields "${line}")
+                    list(LENGTH fields field_count)
+                    if (field_count GREATER 8)
+                        list(GET fields 4 sym_type)
+                        list(GET fields 8 func)
+                        if (sym_type STREQUAL "FUNC" AND func MATCHES "^Java_")
+                            list(APPEND LD_FLAGS "-Wl,--undefined=${func}")
+                        endif ()
+                    endif ()
                 endif ()
             endforeach ()
             string(REGEX REPLACE ";" "\n" LD_FLAGS "${LD_FLAGS}")
