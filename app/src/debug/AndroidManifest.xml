<?xml version="1.0" encoding="utf-8"?>
<!-- DEBUG BUILDS ONLY: the development Aether Call Service is plain HTTP on
     the LAN (see call-service/README.md). Release builds never get this —
     the production call service must be HTTPS. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:usesCleartextTraffic="true" />
</manifest>
